<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Expectations (кастомные матчеры)
|--------------------------------------------------------------------------
*/

// Можно добавить позже, когда появятся доменные сущности:
// expect()->extend('toBeEnabled', fn () => $this->toBeTrue());
// expect()->extend('toBeDisabled', fn () => $this->toBeFalse());

/*
|--------------------------------------------------------------------------
| Helpers (только если реально используются в тестах)
|--------------------------------------------------------------------------
*/

// Фабрики добавляйте ПОСЛЕ создания классов, на которые они ссылаются:
//
// function createFlag(string $name, bool $default = false): FeatureFlag { ... }
// function createContext(array $data = []): EvaluationContext { ... }